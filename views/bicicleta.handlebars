<main class="container min-vh-100">
    <div class="row">
        <div class="col-lg-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <h1 class="card-title">{{capitalize bicileta.marca}}</h1>
                    <h2 class="card-text">{{bicicleta.modelo}}</h2>
                    <p class="mb-5 card-text">Disponible: {{#if bicicleta.disponible}} Si {{else}} No {{/if}}</p>
                </div>
                
            </div>
        </div>
    </div>
    <div class="row my-5">
        <div class="col text-center">
            <a class="btn btn-danger btn-lg" href="/bicicletas">Todas las Bicicletas</a>
        </div>
    </div>
</main>